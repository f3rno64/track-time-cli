import { type YArgsOptionDefinition } from '../types'

const SHEETS_OPTION: YArgsOptionDefinition = [
  'sheets',
  {
    describe: 'Show results for the specified sheets',
    type: 'array'
  }
]

export default SHEETS_OPTION
