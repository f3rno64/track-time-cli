import { type YArgsOptionDefinition } from '../types'

const AT_OPTION: YArgsOptionDefinition = [
  'at',
  {
    describe: 'Check in at a specific time',
    type: 'string'
  }
]

export default AT_OPTION
